# Rahma Push Server

## Setup

Create a `.env` file in this folder with:

```
PUSH_SERVER_PORT=4090
VAPID_PUBLIC_KEY=YOUR_PUBLIC_KEY
VAPID_PRIVATE_KEY=YOUR_PRIVATE_KEY
VAPID_SUBJECT=mailto:you@example.com
```

Install deps and run:

```
npm install
npm start
```
